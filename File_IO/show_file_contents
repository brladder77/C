#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#define MAX 80

int main() {

	FILE* IN;
	char str_Arr[MAX];
	IN = fopen("in.txt", "r");
	while (fgets(str_Arr, MAX, IN) != NULL)
		puts(str_Arr); //화면에 출력하라
	fclose(IN);
  
  return 0;
}
