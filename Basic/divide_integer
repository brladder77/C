#include <stdio.h>

int main()
{
	int a;
  
	printf("정수 천만 이하의 수를입력:");
	scanf_s("%d", &a);
	printf("입력한 수 %d는\n", a);
	printf("%d만 %d천 %d백 %d십 %d입니다.\n", a / 10000, a % 10000 / 1000, a % 1000 / 100, a % 100 / 10, a % 10);
  
  return 0;
  
}
