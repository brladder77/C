#include <stdio.h>

int x, y, n;

int son(x) {
	if (x == 0) return y;
	else return(y++, son(x/10));
}

int main()
{
	printf("정수를 입력하시오 : ");
	scanf_s("%d", &x);
	printf("자리수의 개수: %d", son(x));

	return 0;
}
