#include <stdio.h>

int main() {

	int a, b;
	int* x, * y;
	
  printf("두 개의 정수를 입력하시오 : ");
	scanf_s("%d %d", &a, &b);
	
  x = &a, y = &b;
	printf("두 수의 합은 %d, 곱은 %d 입니다. \n", *x + *y, *x * *y);
  
  return 0;
}
