#include <stdio.h>

int main() {

	int num[2], * p;
	
  printf("두 개의 정수를 입력하시오 :");
	scanf_s("%d %d", &num[0], &num[1]);
	
  p = num;
	printf("두 정수의 합은 %d, 곱은 %d\n", *p + *(p + 1), *p * *(p + 1));
  
}
